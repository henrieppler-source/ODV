# ODV v63

Bitte serverseitig `server/routes_v63.php` als `ortschronik-api/routes.php` hochladen.

Optional für einen sauberen Teststart:

```sql
sql/reset_bewegungsdaten_v63.sql
```

Diese SQL-Datei löscht Dokument-/Punkte-Testdaten, aber keine Benutzer, Rechte, Ortsordner, Punkteregeln oder Verteiler.
